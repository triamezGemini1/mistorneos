# Clubs (CRUD)
- Campos: nombre (único opcional), contacto, estatus.
- Validación: email y teléfono normalizados; nombre 1..255.
- Recomendaciones: paginación, búsqueda por nombre/delegado, subida de `logo` con validación MIME (guardar fuera de `public/`).