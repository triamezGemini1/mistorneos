/**
 * Deshabilitado: antes inyectaba el botón «Volver» junto al breadcrumb.
 * Ya no se carga desde layout.php; el archivo se mantiene por compatibilidad con despliegues antiguos.
 */
(function () {
    'use strict';
})();
