# Registrants (Inscritos)
- Clave única `(torneo_id, cedula)`. FK a `clubs` y `tournaments`.
- Recomendaciones: importación CSV con validación; exportación de reportes por club/edad/género; verificación de mayoría de edad si aplica.